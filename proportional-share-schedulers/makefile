CC = gcc
CFLAGS = -Wall -g -Wextra

ticket:
	$(CC) $(CLFAGS) ticket.c -o ticket

my_process.o:


test:
	$(CC) $(CFLAGS) test.c -o test

all:

clean-ticket:
	rm -f *~ *.o ticket

clean-test:
	rm -f *~ *.o test

clean:
	rm -f *~ *.o ticket
	rm -f *~ *.o test

